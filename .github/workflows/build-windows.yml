name: Build Windows

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - name: Install frontend dependencies
        run: npm install
      - name: Build frontend for Tauri checks
        run: npm run build
      - name: Check Rust backend
        working-directory: src-tauri
        run: cargo check --features dictionary-segmentation
      - name: Test lyrics fallbacks
        working-directory: src-tauri
        run: 'cargo test --features dictionary-segmentation lrclib::'
      - name: Build Windows installer
        run: npm run tauri -- build --features dictionary-segmentation --bundles nsis
      - uses: actions/upload-artifact@v4
        with:
          name: LRCGET-Windows
          path: src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error
